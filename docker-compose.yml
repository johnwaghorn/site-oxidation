services:
  app:
    build:
      context: .
      args:
        - VITE_API_KEY=${API_KEY:-changeme}
    ports:
      - "8081:8080"
    environment:
      - API_KEY=${API_KEY:-changeme}
    volumes:
      - ./data:/app/data
